<%= form_with(model: pfc_ctrl_account_type) do |form| %>
  <% if pfc_ctrl_account_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pfc_ctrl_account_type.errors.count, "error") %> prohibited this pfc_ctrl_account_type from being saved:</h2>

      <ul>
        <% pfc_ctrl_account_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
